import initialState from './initialState'
import {

} from '../constants/actionTypes'

export default (state = initialState.codeExamples, action) => {
    switch (action.type) {

        default:
            return state
    }
}