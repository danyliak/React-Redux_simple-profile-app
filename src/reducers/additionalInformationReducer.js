import initialState from './initialState'
import {

} from '../constants/actionTypes'

export default (state = initialState.additionalInformation, action) => {
    switch (action.type) {

        default:
            return state
    }
}